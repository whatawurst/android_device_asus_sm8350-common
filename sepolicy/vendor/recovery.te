# recovery.te
#
recovery_only(`
    set_prop(recovery, adbd_config_prop)
    set_prop(recovery, adbd_prop)
    set_prop(recovery, boottime_public_prop)
    set_prop(recovery, shell_prop)

    get_prop(recovery, apexd_prop)
    get_prop(recovery, apk_verity_prop)
    get_prop(recovery, bluetooth_a2dp_offload_prop)

    allow recovery pstorefs:dir r_dir_perms;
    allow recovery sysfs:dir r_dir_perms;
    allow recovery sysfs_loop:dir r_dir_perms;
    allow recovery sysfs_loop:file rw_file_perms;

    allow recovery sysfs_io_sched_tuneable:dir r_dir_perms;

    allow recovery sysfs_devices_block:dir r_dir_perms;
    allow recovery sysfs_devices_block:file rw_file_perms;

    allow recovery sysfs_zram:dir r_dir_perms;
    allow recovery sysfs_zram_uevent:file rw_file_perms;
')
