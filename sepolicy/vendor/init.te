# init.te

set_prop(init, exported2_default_prop)

# /ADF
allow init demoapp_file:dir { mounton };

# /asdf
allow init ASDF_file:dir r_dir_perms;

# /batinfo
allow init bat_file:dir { mounton };

# /sys/kernel/tracing
allow init debugfs_tracing_debug:dir { mounton };
